

<div class="container">
  <% unless params[:query] %>
  <div class="greeting row">
    <p>Welcome to <strong>Inner Child</strong>!</p>

    <p>This website is for anyone who dislikes adulting, and would rather
    just sit around and be a kid again.</p>

    <p>Think of a <strong>word</strong> that brought your childhood self joy, and type it in the
      searchbar above. You will be given a personal recommendation of some <strong>candy</strong>, a <strong>video game</strong>, and a <strong>cartoon</strong> based on that word.</p>

    <p>Enjoy the cavities! :D (?)</p>
  </div>
  <% end %>

  <% if params[:query] %>
    <div class="combo row">

      <div class="col-sm-4">
        <div class="panel panel-default">
          <div class="panel-heading">Candy</div>
          <div class="panel-body">
            <%= @candy.name %>
            <%= image_tag @candy.image %>
          </div>
        </div>
      </div>
      <div class="col-sm-4">
        <div class="panel panel-default">
          <div class="panel-heading">Video Game</div>
          <div class="panel-body">
            <%= @game.name %>
            <%= image_tag @game.image %>
          </div>
        </div>
      </div>
      <div class="col-sm-4">
        <div class="panel panel-default">
          <div class="panel-heading">Cartoon</div>
          <div class="panel-body">
            <%= @cartoon.name %>
            <%= image_tag @cartoon.image %>
          </div>
        </div>
      </div>

    </div>
  <% end %>
</div>
